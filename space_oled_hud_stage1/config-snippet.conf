# Keep your existing RGB settings.
CONFIG_ZMK_RGB_UNDERGLOW=y
CONFIG_WS2812_STRIP=y

# Do not let RGB_TOG cut power to the OLEDs.
CONFIG_ZMK_RGB_UNDERGLOW_EXT_POWER=n

# OLED core.
CONFIG_ZMK_DISPLAY=y
CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM=y
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=n

# Use our screen while retaining the nice_oled shield for SSD1306 setup.
CONFIG_SPACE_OLED_HUD=y
CONFIG_NICE_OLED_WIDGET_STATUS=n

# The built-in nice-oled presentation is no longer used, so don't pull in
# its extra presentation features.
CONFIG_NICE_OLED_WIDGET_WPM=n
CONFIG_NICE_OLED_WIDGET_HID_INDICATORS=n
CONFIG_NICE_OLED_WIDGET_MODIFIERS_INDICATORS=n
CONFIG_NICE_OLED_WIDGET_ANIMATION_PERIPHERAL=n
CONFIG_NICE_OLED_WIDGET_STATIC_IMAGE_PERIPHERAL=n
